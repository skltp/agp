<?xml version="1.0" encoding="utf-8"?>

<mule 
	xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:spring="http://www.springframework.org/schema/beans"
	xmlns:cxf="http://www.mulesoft.org/schema/mule/cxf"
	xmlns:jms="http://www.mulesoft.org/schema/mule/jms"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
		http://www.mulesoft.org/schema/mule/core    http://www.mulesoft.org/schema/mule/core/current/mule.xsd
		http://www.mulesoft.org/schema/mule/cxf     http://www.mulesoft.org/schema/mule/cxf/current/mule-cxf.xsd
		http://www.mulesoft.org/schema/mule/jms     http://www.mulesoft.org/schema/mule/jms/current/mule-jms.xsd
	">

    <!-- Test-db for teststubs for source systems -->
    <spring:bean name="service-producer-testdb-bean" class="${SERVICE_PRODUCER_TESTDB_IMPL}">
        <spring:property name="serviceTimeoutMs" value="${SERVICE_TIMEOUT_MS}" />
    </spring:bean>

    <!-- Implementation of teststubs for source systems -->
    <spring:bean name="service-producer-teststub-bean" class="${SERVICE_PRODUCER_TESTSTUB_IMPL}">
        <spring:property name="testDb" ref="service-producer-testdb-bean" />
    </spring:bean>

    <custom-transformer name="testProducerLogger" class="se.skltp.agp.test.producer.TestProducerLogger"/>

	<!-- Teststub service for a source system (källsystem) behind a virtual service in TP VP -->
	<simple-service 
		name="service-producer-teststub-service"
        address="${TESTSTUB_SERVICE_INBOUND_URL}?connector=soitoolkit-http-connector" 
        transformer-refs="testProducerLogger"
        type="jax-ws">
        <component>
            <spring-object bean="service-producer-teststub-bean"/>
        </component>
    </simple-service>

</mule>